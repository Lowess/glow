[default]
neopixel_size = 12
neopixel_pin = 10 # GPIO pin connected to the pixels (10 uses SPI /dev/spidev0.0).
neopixel_freq_hz = 800000 # LED signal frequency in hertz (usually 800khz)
neopixel_dma = 10 # DMA channel to use for generating signal (try 10)
neopixel_brightness = 255 # Set to 0 for darkest and 255 for brightest
neopixel_invert = false # True to invert the signal (when using NPN transistor level shift)
neopixel_channel = 0 # set to '1' for GPIOs 13, 19, 41, 45 or 53

[[default.glow.strips]]
title = "Box1"
start = 0
stop = 6
colors = ["white", "black", "white", "black", "white", "black"]

  # [[default.glow.strips.effects]]
  # name = "bicolor"
  # colors = ["yellow", "blue"]

  [[default.glow.strips.effects]]
  name = "wheel"
  # colors = ["green", "red"]
  clockwise = false

  # [[default.glow.strips.effects]]
  # name = "dim"

[[default.glow.strips]]
title = "Box2"
start = 6
stop = 12
colors = ["red", "red", "blue", "blue", "green", "green"]
  # [[default.glow.strips.effects]]
  # name = "wheel"
  # offset = 2
  [[default.glow.strips.effects]]
  name = "colored_wheel"
  colors = ["green", "red"]

#######################################################################################
### Production
#######################################################################################

[production]
neopixel_size = 74

[[production.glow.strips]]
title = "Box1"
start = 0
stop = 37
colors = ["green", "yellow", "red"]

  [[production.glow.strips.effects]]
  name = "wheel"
  offset=3

[[production.glow.strips]]
title = "Box2"
start = 37
stop = 74
colors = ["none"]

  [[production.glow.strips.effects]]
  name = "colored_wheel"
  colors = ["green", "yellow"]

  [[production.glow.strips.effects]]
  name = "dim"
